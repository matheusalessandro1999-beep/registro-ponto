# Report Page — Finalização

## O que foi feito

### base.css
- Adicionado `<link rel="stylesheet" href="../base.css">` no report.html
- Removido do report.css ~60 linhas duplicadas:
  - `:root` (variáveis já em base.css)
  - Reset `*`, `html`, `body`, `::-webkit-scrollbar`
  - `.main` (margin-left, padding)
  - Toast styles (`.toast-container`, `.toast`, `.toast.ok/warn/error`)
  - `:root { --sidebar-w:... }` nos media queries (já em base.css)
- Mantidos overrides intencionais: `.topbar` (20px), `.page-title` (26px), `.btn` (13px/700), `.form-*`

### Bug real: score ignorava período selecionado
- `calcAssid()` somava TODOS os 12 meses de faltas independente do filtro
- `calcScore()` chamava calcAssid sem período
- Fix: ambas recebem `mesDe, mesAte`, fazem slice no período e ajustam `maxAbs` proporcionalmente
- `_totalAbs` e `calcAssid` agora usam o mesmo slice — consistentes

### Validação de período invertido
- Se `mesDe > mesAte`, exibe toast de erro e aborta (antes calculava com slice vazio → 100% assid pra todos)

### Código morto
- `const store = _store;` removido do `gerarRelatorio` (linha 135, nunca usado)

### Pendências documentadas (arrumar depois/report.md)
- Alinhar fórmula de score com ranking (calcBonus)
- Centralizar funções duplicadas em shared.js
- Fallback rh_ponto em calcAssid

### Estado final dos arquivos
- `pages/report.html` — 106 linhas, limpo, carrega base.css
- `pages/report.css` — 193 linhas, só estilos específicos do relatório
- `pages/report.js` — 367 linhas, com período no calcAssid/calcScore
- `arrumar depois/report.md` — pendências futuras
